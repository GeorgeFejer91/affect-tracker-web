{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "study-window",
  "description": "Use label-guarded native study authority and content-addressed asset-vault commands.",
  "windows": ["study"],
  "permissions": ["core:event:allow-listen"]
}
